# 路线图：三科学习游戏进度

## 阶段一：英语版本 ✅（已完成）

> 单人单科完整可玩，孩子已在用

- [x] 工程架子（Vite + React + Tailwind + HashRouter）
- [x] Supabase 数据库 + 数据读写层
- [x] 登录/注册（session 持久化）
- [x] 多孩子支持（独立进度 + 星星）
- [x] 闯关模式（8题/轮，听音选图 + 字母填空交替）
- [x] 单词记忆模式（选词→播报→填空练习）
- [x] 四个角色系统（龙/霸王龙/孙悟空/老虎）
- [x] 星星奖励系统（累计+可用分离 + 连击额外奖励）
- [x] 等级颜色系统（HSL 三段渐变）
- [x] 家长单词解锁（按单元展开）
- [x] 家长奖励管理（模板 + 兑换记录 + 冷却）
- [x] 学习统计（概览 + 正确率趋势 + 错词排行 + 单元进度）
- [x] 离线队列重传（localStorage + 30s 轮询）
- [x] 字母填空 Multiset（重复字母正确分配槽位）
- [x] 游戏记录去重（clientSessionId UNIQUE）

---

## 阶段二：英语对话模式 ✅（旧版，已迁移）

> ~~基于现有角色和词库，增加情景对话练习~~

- ~~[x] 对话题库（15 场景，覆盖 6 单元 + 跨单元综合）~~
- [x] DialogueBubble 组件（NPC 对话气泡 + 打字机效果）
- [x] ChoicePanel 组件（选项按钮 + 正确/错误反馈）
- [x] DialoguePractice 页面（完整的对话模式）
- [x] 路由 `/dialogue`（已注册到路由表）
- [x] 入口按钮（StartScreen 三按钮并列：单词本/对话/开始游戏）
- [x] 评分 + 星星入库（复用 scoring.js）
- [x] STRINGS 文案（对话模式相关文字标签）
- [x] 中英双语显示（NPC 句子 + 选项均配中文翻译）
- [x] 语音朗读（Web Speech API，NPC 句子自动播 + 点击选项播）
- [x] 8 轮/局（从多个对话场景拼满 8 轮）
- [x] 进度圆点（同闯关模式，已答绿色/红色标记）
- [x] 选项随机打乱（正确答案不固定位置）
- [x] 语音优选（按平台选最佳英文语音）
- [x] 鸿蒙兼容（无英文语音时使用系统默认引擎）
- [x] 背景统一（使用 game 答题页紫色渐变风格）
- [x] 共享 TTS 模块（`tts.js`，Bubble 和选项面板共用）

---

## 阶段三：语文 + 数学 ✅（题目已生成）

> 多科架构改造完成，题型 demo 通过，**所有题目 JSON 已由 AI 直接生成**（无需手动填写课文）

### ✅ 已完成

#### 架构改造
- [x] 三科统一配置层（`subjects.js` — subject + grade 双维度，支持 1~6 年级）
- [x] 缓存 key 通用化（`eng_` → `app_` / `{subject}_g{grade}_`，涉及 9 个文件）
- [x] DB 迁移脚本（三张表加 `subject` + `grade` 列，扩展主键）
- [x] 孩子首页 `/home`（玻璃卡风格，含年级选择行 + 三科卡片）
- [x] SelectChild 导航改为 `/home`
- [x] 家长面板按科管理（科目标签切换，解锁+统计按科过滤）
- [x] 课后各科页面接 subject/grade 参数（Game / Memory / Dialogue 均支持）

#### 语文题型
- [x] `ChineseReadingChoice` 组件（阅读短文→4选1，含5阶标签）
- [x] ~~样题 8 道~~ → **三年级上下册共 16 单元 × 8 题 = 128 题已生成**
- [x] Demo 页面 `/demo-chinese` 可独立运行

#### 数学题型
- [x] `MathChoice` 组件（纯文字应用题→选算式/选同类，CPA 四阶）
- [x] ~~样题 8 道~~ → **三年级上下册共 17 单元 × 8 题 = 136 题已生成**
- [x] Demo 页面 `/demo-math` 可独立运行

#### 遗留问题修复
- [x] 单词进度接入游戏（从 DB 加载真实 wordProgressMap）
- [x] DataPrefetch 预加载所有孩子数据
- [x] FeedbackOverlay onComplete 替代独立 setTimeout
- [x] submitAnswer 返回值已捕获
- [x] WordMemory 星星走 StarContext.addStars（离线兜底）

#### 新管道内容生成
- [x] 语文题目 JSON（`src/lib/chinese/courses/`）：三年级上下册 16 单元 128 题
- [x] 数学题目 JSON（`src/lib/math/courses/`）：三年级上下册 17 单元 136 题
- [x] 英语对话 JSON（`src/lib/english/courses/`）：三年级上下册 12 单元 96 轮对话
- [x] **英语对话已迁移到新管道** → `DialoguePractice` 从 `src/lib/english/courses/` 加载 AI 生成对话 JSON

---

## 阶段四：新管道迁移 ✅（已完成）

> 建立双管道架构：英语旧链路不动，新内容走 AI 生成

### 架构决策

**旧链路（英语闯关/单词记忆）：** 210 个手写单词 + 预录 MP3 + 预生成 webp 配图 + questionEngine 手动出题。**保持运行，不改动。**

**新管道（语文/数学/英语对话）：** AI agent 批量生成内容 → JSON 存于 `src/lib/{subject}/courses/` → 游戏时直接 import。语音用 Web Speech API TTS，不需要配图。不需要"单词解锁"概念。不需要"单词进度"。

**远期：** 英语闯关/记忆也会迁到新管道，届时可下线 MP3 + webp 资源。

### ✅ 已完成

- [x] **家长面板适配** → UnlockPanel 固定英语，ErrorRanking/UnitProgress 按科切换
- [x] **英语对话接入新管道** → `DialoguePractice` 从 `src/lib/english/courses/` 加载对话 JSON
- [x] **语文/数学接入完整游戏流程** → GameContext 按 subject 路由，渲染多题型

### ⏳ 后续

- [ ] **加四年级/五年级/六年级内容** → 按同样方式由 AI 生成
