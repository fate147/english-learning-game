# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/AppData/Roaming/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[builtin_mcp]
time_enabled = true   # built-in Time MCP; off until manually enabled
context7_enabled = false   # built-in Context7 MCP; off until manually enabled

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && npm run build)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && npm run deploy)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git remote -v && echo \"---\" && cat .git/config | grep -A 3 \"remote\")", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git log origin/gh-pages -1 --oneline && echo \"---\" && git diff master..origin/gh-pages --name-only 2>/dev/null || echo \"gh-pages not fetched\")", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git fetch origin gh-pages && git log origin/gh-pages -1 --oneline)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git rm -r --cached dist 2>/dev/null; echo \"---\" ; npm run build)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git status --short)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git log --oneline -1)", "Bash(cd /c/Users/89659/Documents/DeepSeek/english-learning-game && git add package.json && git commit -m \"fix: deploy 脚本改为 gh-pages -d docs\" && git push)", "Bash(mkdir -p:*)", "Edit", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && npx vite --host)", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && node -e \"\nconst fs = require('fs');\nconst path = require('path');\nconst dir = 'src/lib/chinese/courses';\nconst files = fs.readdirSync(dir).filter(f => f.endsWith('.json') && f !== 'index.js');\n\nfiles.forEach(f => {\n  const data = JSON.parse(fs.readFileSync(path.join(dir, f), 'utf8'));\n  data.forEach(q => {\n    // Check if passage or question contains '这个单元' or '本单元' or '这篇课文' without specific reference\n    if (/这个单元|本单元|这篇课文|这首诗|这段话/.test(q.passage || '') || /这个单元|本单元/.test(q.question || '')) {\n      console.log(f + ' | ' + q.id + ' | stage:' + q.stage);\n      console.log('  问题:', q.question?.slice(0, 80));\n      console.log('  段落前60字:', q.passage?.slice(0, 80));\n      console.log('');\n    }\n  });\n});\n\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && node -e \"\nconst fs = require('fs');\nconst path = require('path');\nconst dir = 'src/lib/chinese/courses';\n\nconst SEMESTER_MAP = { s1: '上册', s2: '下册' };\n\nconst files = fs.readdirSync(dir).filter(f => f.endsWith('.json') && f !== 'index.js');\nlet total = 0;\n\nfiles.forEach(f => {\n  const match = f.match(/g3_(s[12])_u(\\d+)\\.json/);\n  if (!match) return;\n  const sem = SEMESTER_MAP[match[1]];\n  const unit = match[2];\n  const unitLabel = '三年级' + sem + '第' + unit + '单元';\n\n  const filePath = path.join(dir, f);\n  let content = fs.readFileSync(filePath, 'utf8');\n  \n  // Replace '这个单元' in passage fields\n  // Only replace when it's the start of the passage or after a period\n  const count = (content.match(/这个单元/g) || []).length;\n  if (count > 0) {\n    content = content.replace(/这个单元/g, unitLabel);\n    fs.writeFileSync(filePath, content, 'utf8');\n    console.log(f + ': 替换 ' + count + ' 处 → ' + unitLabel);\n    total += count;\n  }\n});\n\nconsole.log('共替换 ' + total + ' 处');\n\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && node -e \"\nconst fs = require('fs');\n\n// Check a few examples\n['g3_s2_u1.json', 'g3_s1_u4.json'].forEach(f => {\n  const data = JSON.parse(fs.readFileSync('src/lib/chinese/courses/' + f, 'utf8'));\n  data.filter(q => q.stage === 5 || q.stage === 4).forEach(q => {\n    console.log(f + ' | ' + q.id + ' | stage:' + q.stage);\n    console.log('  passage:', q.passage.slice(0, 100));\n    console.log('  question:', q.question.slice(0, 80));\n    console.log('');\n  });\n});\n\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && npx vitest run src/lib/chinese 2>&1)", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && node -e \"\nconst { WORDS } = require('./src/lib/words.js');\nconst grades = new Set();\n// English words have units, but not explicit grades\n// Let me check what word data looks like\nconsole.log('Total words:', WORDS.length);\nconsole.log('Sample word:', JSON.stringify(WORDS[0]));\n// Check units range\nconst units = new Set(WORDS.map(w => w.unit));\nconsole.log('Units:', [...units].sort().join(', '));\n\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && git add -A && git commit -m \"UI打磨 + 科目主题 + vitest验证 + 语文数学接入游戏流程\n\n- 科目主题色：英语紫、语文茶绿、数学雾蓝，每科独立渐变色\n- CSS：玻璃面板不透明度提升，文字阴影增强可读性\n- 键盘焦点样式 + prefers-reduced-motion 动画降级\n- vitest 测试：数学三层验证（结构+算术验算+CPA框架）\n- 语文/数学结构校验 + 英语对话结构校验（19个测试全通过）\n- AI数据修复：4处多标正确答案、7处干扰项标记备查\n- 语文数学从Demo页面迁移至Game.jsx，随机抽题\n- 语文题目'这个单元'替换为具体单元名称\n- 年级范围统一为三年级（其他年级暂缺）\n- 加载状态、入场动画、LetterFill disabled修复\n- DialogueBubble图片错误处理改为React state\n- 角色选择页移除重复的对话/单词本按钮\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && git add -A -- \":!NUL\" && git status --short | findstr /R \"^??\")", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && git rm --cached NUL 2>/dev/null; rm -f NUL 2>/dev/null; git add -A 2>&1 | tail -3)", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && git status --short | wc -l)", "Bash(cd \"C:\\Users\\89659\\Documents\\DeepSeek\\english-learning-game\" && echo \"NUL\" >> .gitignore && git add -A . && git commit -m \"UI打磨 + 科目主题 + vitest验证 + 语文数学接入游戏流程\")"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
allow_write = ["C:\\Users\\89659\\Documents\\Obisdians\\PersonalNotes\\项目\\英语学习游戏"]
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
